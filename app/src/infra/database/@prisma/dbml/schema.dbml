//// ------------------------------------------------------
//// THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
//// ------------------------------------------------------

Table credentials {
  id String [unique, not null]
  nullifier String [unique]
  issuer String
  kycProvider String
  kycApproved Boolean
  issuedAt DateTime
  vcHash String [unique, not null]
  cpfDedupKey String [unique]
  issuerDid String [not null]
  issuerId String [not null]
  subjectDid String [not null]
  kycLevel String [not null]
  status CredentialStatus [not null, default: 'ACTIVE']
  sorobanTxHash String
  userWalletAddress String
  privyUserId String
  createdAt DateTime [not null]
  updatedAt DateTime [not null]
  expiresAt DateTime
}

Table attestation {
  id String [unique, not null]
  vcHash String [not null]
  proofHash String [not null]
  verifierId String [not null]
  kycLevel String [not null]
  sorobanTxHash String
  sorobanLedger Int
  onChainResult Boolean [not null]
  issuerId String
  userWalletAddress String
  createdAt DateTime [not null]
}

Table commission_ledger_entries {
  id String [unique, not null]
  issuerId String [not null]
  attestationId String [unique]
  entryType CommissionEntryType [not null, default: 'ACCRUAL']
  status CommissionEntryStatus [not null, default: 'PENDING_SECURITY']
  amountMinor Int [not null]
  currency String [not null, default: 'BRL']
  source String [not null, default: 'ATTESTATION_REUSE']
  occurredAt DateTime [not null]
  availableAt DateTime [not null]
  payoutCycleId String
  settledAt DateTime
  reversalOfId String
  createdAt DateTime [not null]
}

Table payout_cycles {
  id String [unique, not null]
  periodStart DateTime [not null]
  periodEnd DateTime [not null]
  cutoffAt DateTime [not null]
  status PayoutCycleStatus [not null, default: 'PREVIEW']
  createdAt DateTime [not null]
  updatedAt DateTime [not null]

  indexes {
    (periodStart, periodEnd, cutoffAt) [unique]
  }
}

Table payout_cycle_items {
  id String [unique, not null]
  payoutCycleId String [not null]
  issuerId String [not null]
  amountMinor Int [not null]
  entriesCount Int [not null]
  entryIds Json [not null]
  blockedReason String
  walletAddress String
  createdAt DateTime [not null]

  indexes {
    (payoutCycleId, issuerId) [unique]
  }
}

Table organization_wallets {
  id String [unique, not null]
  issuerId String [unique, not null]
  privyUserId String [unique]
  privyWalletId String
  stellarAddress String [unique]
  network String [not null]
  status OrganizationWalletStatus [not null, default: 'PENDING']
  accountActivated Boolean [not null, default: false]
  trustlineReady Boolean [not null, default: false]
  assetCode String [not null]
  assetIssuer String
  lastError String
  createdAt DateTime [not null]
  updatedAt DateTime [not null]
}

Table issuer {
  id String [unique, not null]
  issuerId String [unique, not null]
  name String [not null]
  status String [not null, default: 'active']
  publicKey String
  privyEnabled Boolean [not null, default: false]
  createdAt DateTime [not null]
}

Table backoffice_users {
  id String [unique, not null]
  issuerId String [not null]
  email String [unique, not null]
  passwordHash String [not null]
  name String
  active Boolean [not null, default: true]
  createdAt DateTime [not null]
  updatedAt DateTime [not null]
}

Table verifier {
  id String [unique, not null]
  name String [not null]
  status String [not null, default: 'active']
  createdAt DateTime [not null]
  updatedAt DateTime [not null]
}

Table ingress {
  id String [unique, not null]
  request Json [not null]
  response Json [not null]
  createdAt DateTime [not null]
}

Table egress {
  id String [unique, not null]
  request Json [not null]
  response Json [not null]
  createdAt DateTime [not null]
}

Table api_keys {
  id String [unique, not null]
  issuerId String
  key String [unique, not null]
  name String [not null]
  active Boolean [not null, default: true]
  createdAt DateTime [not null]
  revokedAt DateTime
}

Enum CredentialStatus {
  ACTIVE
  REVOKED
  EXPIRED
  PENDING
  REJECTED
}

Enum CommissionEntryType {
  ACCRUAL
  REVERSAL
}

Enum CommissionEntryStatus {
  PENDING_SECURITY
  AVAILABLE
  ALLOCATED
  SETTLED
  REVERSED
}

Enum PayoutCycleStatus {
  PREVIEW
}

Enum OrganizationWalletStatus {
  PENDING
  ACTIVE
  ERROR
  SUSPENDED
}